# Core and CLI dependencies
huggingface_hub>=0.18.0
structlog>=23.0.0
humanize>=4.0.0
tabulate>=0.9.0
requests

# For web UI, install separately using: pip install Flask>=2.0.0
# Or install with the 'web' extra: pip install .[web]